#pragma once

namespace card {

    /// Contains the relevant information to update the round with a number of cards
    class CardContext {
        /// Number of cards requested by the current Round per frame
        int card_requests = 0;


    };
}