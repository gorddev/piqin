#pragma once


namespace gfx {
    /// Renders a shadow on to a specified floor point. Floor is stored in the shadowBank.
    inline void shadow_floor(std::vector<SDL_Vertex>& buffer, int& numVertices, void* userdata) {

    }
}
