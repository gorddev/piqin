#pragma once

namespace geng::physics {

    /**
     * @brief The Physics Layer is a special kind of layer well suited to handling collisions and physics updates.
     * @details The Physics Layer goes through a special initialization process by decoding .lvl files. Thus, there
     * are specialized member classes specific to the physic layer that can be found in scene/physics
     */
}